[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "legal-ai-backend"
version = "1.0.0"

[tool.scripts]
app = "uvicorn app:app --host 0.0.0.0 --port $PORT"

[tool.vercel]
builder = "vercel/python"
